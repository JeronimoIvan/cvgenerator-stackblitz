import {
  Box,
  Divider,
  MenuItem,
  MenuList,
  Popover,
  Typography,
} from '@mui/material';

const DataTypePopover = () => {
  return <Popover>Hello Popover</Popover>;
};

export default DataTypePopover;
